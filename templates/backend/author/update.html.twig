{% extends 'base.html.twig' %}

{% block title %}
    Modification de l'Auteur | {{ parent() }}
{% endblock %}

{% block body %}
    <section class="container mt-4">
        <h1 class="text-center">Modification de l'Auteur {{ author.fullName }}</h1>
        {% include 'backend/author/_form.html.twig' with {buttonLabel: 'Modifier'} %}
        <a href="{{ path('admin.authors.index') }}" class="btn btn-primary mt-4">Retour à la liste</a>
    </section>
{% endblock %}

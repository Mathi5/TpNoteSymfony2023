{% extends 'base.html.twig' %}

{% block title %}
    Modification de la catégorie | {{ parent() }}
{% endblock %}

{% block body %}
    <section class="container mt-4">
        <h1 class="text-center">Modification de la catégorie {{ category.title }}</h1>
        {% include 'backend/category/_form.html.twig' with {buttonLabel: 'Modifier'} %}
        <a href="{{ path('admin.categories.index') }}" class="btn btn-primary mt-4">Retour à la liste</a>
    </section>
{% endblock %}

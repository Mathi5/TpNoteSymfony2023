{% extends 'base.html.twig' %}

{% block title %}Gestion des livres | {{ parent() }}{% endblock %}

{% block body %}
    <section class="container mt-4">
        <h1 class="text-center">Administration des articles</h1>
        <a href="{{ path('admin.books.create') }}" class="btn btn-primary"><i class="bi bi-file-earmark-plus"></i> Ajouter un livre</a>
        <div class="blog-card-list mt-4">
            {% for book in books %}
                {% include 'Components/_card.html.twig' %}
            {% endfor %}
        </div>
    </section>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}
    Modification d'un livre | {{ parent() }}
{% endblock %}

{% block body %}
    <section class="container mt-4">
        <h1 class="text-center">Modification d'un article {{ book.title }}</h1>
        {% include 'backend/book/_form.html.twig' with {buttonLabel: 'Modifier'} %}
        <a href="{{ path('admin.books.index') }}" class="btn btn-primary mt-4">Retour à la liste</a>
    </section>
{% endblock %}

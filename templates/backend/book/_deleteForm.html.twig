<form action="{{ path('admin.books.delete', {id: book.id}) }}" method="post"
      onsubmit="return confirm('Ête-vous sur de vouloir supprimer ce livre ?')">
    <input type="hidden" name="token" value="{{ csrf_token('delete' ~ book.id) }}">
    <button type="submit" class="btn btn-danger text-light"><i class="bi bi-trash3"></i> Supprimer</button>
</form>